pipeline {
    agent any
    stages {
        stage('Install Packages') {
            steps {
                sh 'python3 pip3 install -r requirements.txt'
            }
        }
        stage('Run Code') {
            steps {
                sh 'python main.py'
            }
        }
    }
}
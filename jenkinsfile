pipeline{
    agent any
    stages{
        steps("installing packages"){
            sh ."pip install -r requirements.txt"
        }
        steps("running the code"){
            sh. "python main.py"
        }
    }
}
pipeline{
    agent any
    stages{
        stage("installing packages"){
            sh ."pip install -r requirements.txt"
        }
        stage("running the code"){
            sh. "python main.py"
        }
    }
}
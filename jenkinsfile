pipeline {
    agent any
    stages {
        stage('Install Packages') {
            steps {
                sh 'pip install -r requirements.txt'
            }
        }
        stage('Run Code') {
            steps {
                sh 'python main.py'
            }
        }
    }
}